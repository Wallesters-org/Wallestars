# 🚀 Настройка на Wallestars Control Center

## Изисквания за среда

Този проект е конфигуриран за работа в **Docker Dev Container** за оптимална среда на разработка.

### Опция 1: Използване на Dev Container (Препоръчително)

#### Стъпки за настройка:

1. **Инсталирайте необходимите инструменти:**
   - [Docker Desktop](https://www.docker.com/products/docker-desktop/) за Windows
   - [Visual Studio Code](https://code.visualstudio.com/)
   - Разширение [Dev Containers](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-containers)

2. **Стартирайте Docker Desktop**

3. **Отворете проекта в VS Code:**
   ```bash
   code .
   ```

4. **Отворете в Dev Container:**
   - Натиснете `F1` или `Ctrl+Shift+P`
   - Изберете: `Dev Containers: Reopen in Container`
   - Изчакайте контейнерът да се създаде (първоначално може да отнеме няколко минути)

5. **Проверете, че всичко е настроено:**
   ```bash
   node --version  # Трябва да покаже v20.x
   npm --version   # Трябва да покаже версията на npm
   ```

6. **Конфигурирайте .env файла:**
   - Файлът вече е създаден автоматично
   - Добавете вашия Anthropic API ключ:
   ```bash
   nano .env
   ```
   Променете:
   ```env
   ANTHROPIC_API_KEY=вашият_api_ключ_тук
   ```

7. **Стартирайте проекта:**
   ```bash
   npm run dev
   ```

8. **Отворете в браузър:**
   - Frontend: http://localhost:5173
   - Backend API: http://localhost:3000
   - WebSocket: http://localhost:3001

### Опция 2: Локална настройка (Без Docker)

Ако предпочитате да не използвате Docker, следвайте стандартните инструкции от [README.md](README.md):

1. Инсталирайте **Node.js 20.x** или по-нова версия
2. Инсталирайте зависимостите: `npm install`
3. Конфигурирайте `.env` файл
4. Стартирайте: `npm run dev`

**Забележка:** За Linux Computer Use функциите ще трябва:
- Linux операционна система
- `xdotool`: `sudo apt install xdotool`

За Android Control функциите:
- Android SDK Platform Tools
- `adb` в PATH

## Решаване на проблеми

### Docker контейнерът не стартира
- Уверете се, че Docker Desktop е стартиран
- Проверете, че имате достатъчно дисково пространство
- Рестартирайте Docker Desktop

### Портове вече са заети
Променете портовете в `.env`:
```env
PORT=3002
WS_PORT=3003
```

### npm install грешки
В Dev Container всички зависимости се инсталират автоматично. Ако има проблем:
```bash
rm -rf node_modules package-lock.json
npm install
```

## Полезни VS Code команди в Dev Container

- `F1` → `Dev Containers: Rebuild Container` - Изграждане наново на контейнера
- `F1` → `Dev Containers: Reopen Folder Locally` - Връщане към локална среда
- `Terminal` → `New Terminal` - Нов терминал вътре в контейнера

## Допълнителна информация

- [Dev Containers документация](https://code.visualstudio.com/docs/devcontainers/containers)
- [Docker Desktop документация](https://docs.docker.com/desktop/)
